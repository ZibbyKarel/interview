/**
 * Just a simple function to create a theme object
 *
 * @returns {object} - Theme object
 */
export const createTheme = () => {
	return {
		// some palete colors
		palette: {
			error: '#ff6262',
		},
		// some tokens
		tableRowColorPrimary: 'white',
		tableRowColorSecondary: 'rgba(0, 0, 0, 0.04)',
	};
};
